Here are three rewritten versions of the sentence, each with a slightly different tone and format:

**Version 1: Formal**

Critical Guidelines for Unit Command:

1. Only issue commands to units labeled as "Ally". Units marked as "Enemy" or "Neutral" should not be commanded.
2. Each move must adhere to the following JSON structure, including all four essential fields:
	* unit_position
	* unit_id
	* new_unit_position
	* command_type
3. The unit's current position in the game state must exactly match the position specified in the unit_position field.

**Version 2: Brief**

Critical Rules:

1. Only command Ally units (no Enemy or Neutral).
2. Each move must be a valid JSON object with these four fields:
	* unit_position
	* unit_id
	* new_unit_position
	* command_type
3. Unit positions must match exactly as listed in the game state.

**Version 3: Instructional**

To ensure successful moves, follow these critical rules:

1. When commanding units, only target those marked "Ally". Leave Enemy and Neutral units untouched.
2. Each move requires a JSON object with four essential fields:
	* unit_position (the current position of the unit)
	* unit_id (the ID of the unit being commanded)
	* new_unit_position (the desired position for the unit)
	* command_type (the type of action to take, e.g., move or attack)
3. Double-check that the unit's position in your command matches its current position in the game state exactly.